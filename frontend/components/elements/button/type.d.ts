interface buttonInterface {
  text: string,
  background: string,
  usage?: () => void,
}

interface styledButtonInterface {
  background: string,
}

interface submitButtonInterface {
  text: string,
}